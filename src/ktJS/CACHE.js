const container = null // container
let _myMeshArr = []
const pixelStream = null // UE 像素流
const group = new Bol3D.Group()
group.name = 'group'
group.position.set(-300, 20, 650)
export const CACHE = {
  container,
  _myMeshArr,
  pixelStream,
  group,
}
